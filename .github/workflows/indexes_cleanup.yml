name: Cleanup old indexes
on: [push]
jobs:
  Deployment-Task:
    runs-on: ubuntu-latest
    steps:
      - name: Connect to PostgreSQL
          run: curl --version
          env:
            POSTGRES_HOST: postgres
            POSTGRES_PORT: 5432
          shell: bash
