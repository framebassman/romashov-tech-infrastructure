# Module retained only so the aiven provider configuration in provider.tf
# stays available while terraform destroys the orphaned aiven_mysql_* state
# entries. Remove this entire directory in the follow-up PR after apply.
